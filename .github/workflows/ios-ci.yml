name: iOS CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

concurrency:
  group: ios-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (Simulator)
    runs-on: macos-26
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.5"

      - name: Show toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: Cache Swift Package Manager
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/org.swift.swiftpm
            ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
          key: spm-${{ runner.os }}-xcode-26.5-${{ hashFiles('LeVestiaire.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-xcode-26.5-

      - name: Resolve Swift packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project LeVestiaire.xcodeproj \
            -scheme LeVestiaire

      - name: Build Debug (simulator)
        run: ./Scripts/ci_build.sh

      - name: Run unit and UI tests
        run: ./Scripts/ci_test.sh

      - name: Build Release (simulator)
        env:
          CONFIGURATION: Release
        run: ./Scripts/ci_build.sh
